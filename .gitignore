/target
/web/node_modules
/web-next/node_modules
# Vitest writes its cache to the nearest node_modules, which is the repo root
# when a run is started from here rather than from web-next/.
/node_modules
__pycache__/
/web-mockup

# the reference rig builds intro-skipper into ~/.cache; these are dotnet scratch
scripts/introref/obj/
scripts/introref/bin/
